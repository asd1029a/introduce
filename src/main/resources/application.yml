server:
  port: 8880


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:4406/introduce?serverTimezone=UTC
    username: 'root'
    password: 'root'
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: false
    show-sql: true
    database: mysql
    database-platform: 'org.hibernate.dialect.MySQL5InnoDBDialect'
    properties:
      hibernate:
